universe = vanilla

# Files
output = out/data.$(cloud_region).$(cluster).aws.$(process).out
error = err/data.$(cloud_region).$(cluster).aws.$(process).err
log = condor.log

# File transfer behavior
ShouldTransferFiles = YES
WhenToTransferOutput = ON_EXIT

#requirements = (SlotID <6)
requirements=(CLOUD_Region =?= "$(cloud_region)")

executable = stash_test.sh
#
# arguments = server-region nmax

arguments = awsuswest 24
queue
arguments = awsuseast 24
queue

