# Condor config for GCP Invalid setup
#

# connect to one that I think exists
CLOUD_HEAD = 35.209.94.45

CLOUD_Provider = "Google"
CLOUD_Region = "GCPInvalid"
GEO_Region ="Unknown"

DEFAULT_DOMAIN_NAME = invalid.gcp 

START = (DEBUG_JOB =?= true)
